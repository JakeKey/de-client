import styled from "styled-components";

import colors from "styles/colors";

const Text = styled.span`
  color: ${colors.black};
  text-align: center;
  margin-bottom: 10px;
`;

export default Text;
